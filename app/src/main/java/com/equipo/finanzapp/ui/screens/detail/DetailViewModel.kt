package com.equipo.finanzapp.ui.screens.detail

import androidx.lifecycle.ViewModel
import com.equipo.finanzapp.data.repository.MainRepository

class DetailViewModel(private val repository: MainRepository) : ViewModel() {
    // ViewModel logic for Detail
}